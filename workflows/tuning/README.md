Output from the `trial.py` script appears in `outdir/jobs`. For whatever reason,
Sherpa will not refresh these output files if they already exist - you need to delete
them in order to get new output.

