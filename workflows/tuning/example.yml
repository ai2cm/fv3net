algorithm:
  name: GridSearch
  kwargs:
    num_grid_points: 2
parameters:
  - name: training_timesteps
    type: choice
    range: [2]
  - name: timesteps_per_batch
    type: discrete
    range: [1, 2]
    scale: linear
