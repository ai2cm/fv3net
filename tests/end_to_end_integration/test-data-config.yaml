mapper_config:
  function: open_nudge_to_fine
  kwargs:
    data_path: gs://vcm-ml-code-testing-data/c48-nudged-run-for-e2e/fv3gfs_run
    nudging_variables:
      - air_temperature
      - specific_humidity
      - x_wind
      - y_wind
      - pressure_thickness_of_atmospheric_layer
# note this differs from the training config in that
# there is no unstacked_dims arg here
timesteps_per_batch: 2
timesteps:
  - "20160801.004500"
  - "20160801.011500"
