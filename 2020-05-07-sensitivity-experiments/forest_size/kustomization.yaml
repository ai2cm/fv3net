apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
bases: 
  - "../base"
nameSuffix: "-change-forest-size"  # bash script that iterates over num trees should affix that info to this suffix
configMapGenerator:
- files:
    - train_sklearn_model.yml
  name: end-to-end
  literals:
    - JOBNAME=change-forest-size
  behavior: merge # merges with the base configuration generator