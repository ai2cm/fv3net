apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
bases: 
  - "../base"
nameSuffix: "-train-data-segment"
configMapGenerator:
- files:
    - end_to_end.yml
  name: end-to-end
  literals:
    - JOBNAME=train-data-segment
  behavior: merge # merges with the base configuration generator