apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
bases: 
  - "../../base"
nameSuffix: "-advisory-council-comparison"
configMapGenerator:
- files:
    - end_to_end.yaml
  name: end-to-end
  literals:
    - JOBNAME=advisory-council-comparison
  behavior: merge # merges with the base configuration generator