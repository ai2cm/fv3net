apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
bases: 
  - "../base"
nameSuffix: "-integration-test"
configMapGenerator:
- files:
    - time-control.yaml
  name: end-to-end
  behavior: merge # merges with the base configuration generator
