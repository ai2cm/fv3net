apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
bases: 
  - "../base"
nameSuffix: "-physics-off"
configMapGenerator:
- files:
    - one_step_jobs.yml
  name: end-to-end
  literals:
    - JOBNAME=physics-off
  behavior: merge # merges with the base configuration generator
