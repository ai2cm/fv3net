apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
bases: 
  - "../base"
nameSuffix: "-physics-on"
configMapGenerator:
- files:
    - one_step_jobs.yml
  name: end-to-end
  literals:
    - JOBNAME=physics-on
  behavior: merge # merges with the base configuration generator
