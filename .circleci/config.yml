version: 2.1
jobs:
  build:    
    docker:
    - image: continuum/miniconda:latest
      environment:
          GOOGLE_APPLICATION_CREDENTIALS=key.json
    # machine: true
    steps:
    # - checkout
    - run: |
        echo "$ENCODED_GCR_KEY" | base64 --decode 
    # - run: conda install make
    # - run: make update_submodules
    # - run: make create_environment
    # - run: | 
    #     source activate fv3net
    #     pytest -m "not regression" --mpl --mpl-baseline-path=tests/baseline_images
  lint:
    docker:
    - image: circleci/python:3.7
      environment:
          GOOGLE_APPLICATION_CREDENTIALS=key.json
    steps:
    - checkout
    - run: sudo pip install black flake8
    - run: make lint
workflows:
  version: 2
  test_and_lint:
    jobs:
      - lint
      - build
