version: 2.1
orbs:
jobs:
  build:
    docker:
    - image: continuumio/miniconda3:4.7.12
    steps:
    - checkout
    - run: conda env create --name test environment.yml
    - run: export PATH=/opt/conda/envs/test/bin:$PATH
    - run: snakemake -n -p .
    - run: pytest
