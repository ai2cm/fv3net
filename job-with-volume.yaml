apiVersion: batch/v1
kind: Job
metadata:
  name: fine-res-q1-q2
spec:
  template:
    spec:
      containers:
        - name: main
          image: us.gcr.io/vcm-ml/prognostic_run
          command: ["bash", "-c", "-x"]
          args:
            - |
              ls
      restartPolicy: Never
  backoffLimit: 0
