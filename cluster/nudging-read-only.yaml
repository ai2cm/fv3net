apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nudging-read-only
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: microk8s-hostpath
  resources:
    requests:
      storage: 50Gi
