Generate kubernetes manifests for an experiment
Run this script from the root directory of the project

usage:

end_to_end/generate.sh <configName> <prognotic_run_image> <fv3net_image>

configName is a folder in end_to_end/configs
