apache-beam[gcp]
zarr>=2.5.0
poetry
nc-time-axis
bump2version
yq
pytest-regtest
conda-lock
pip-tools
tox

# This version of gcsfs breaks logging
gcsfs!=0.7.0

# this version has wheels = faster installs
numcodecs>=0.7.2
