channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - cartopy
  - conda-build
  - crcmod
  - ffmpeg
  - gfortran
  - partd
  - pyquery>=1.4.0
  # testing
  - coverage>=5
  # newer pip version 20.3 does not work when constraints have extras
  - pip #==20.2.4
  - setuptools #==59.8.0  # newer setuptools gives error when running lock_pip. GH#1924
