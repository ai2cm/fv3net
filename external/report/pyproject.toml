[tool.poetry]
name = "report"
version = "0.1.0"
description = ""
authors = ["Oliver Watt-Meyer <oliwm@vulcan.com>"]

[tool.poetry.dependencies]
jinja2 = "^2.11"
python = "^3.7"
pytz = "^2019.3"

# optional deps
holoviews = { version="^1.13", optional=true }

[tool.poetry.extras]
holoviews = ["holoviews"]

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
