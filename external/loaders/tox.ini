[tox]
envlist = py38

[testenv]
deps = 
    -c ../../constraints.txt
    ../vcm
    ../synth
    -e .
passenv = 
    GOOGLE_APPLICATION_CREDENTIALS 
    NIX_SSL_CERT_FILE
commands = pytest {posargs}

