- name: GraphHyperparameters
  config:
    random_seed: 0
    model_type: graph
    hyperparameters:
      loss:
        loss_type: mse
      graph_network:
        in_feats:
        num_step: 1
        n_hidden: 256
        out_feats: 2
        dropout: 0
      training_loop:
        batch_size: 1
        epochs: 2
      optimizer_config:
        name: AdamW
        learning_rate: 0.001
      input_variables:
        - a
      output_variables:
        - a