import tensorflow as tf
import numpy as np
import os
import logging

# from fv3fit.keras._models import DenseWithClassifier

# MODEL_PATH = os.environ.get("TKE_EMU_MODEL")
# # MODEL_FILENAME = "model.tf"
# # X_PACKER_FILENAME = "X_dict_packer.json"
# # Y_PACKER_FILENAME = "y_dict_packer.json"

# model = DenseWithClassifier.load(MODEL_PATH)


def tke_emulator(state):

    logger = logging.getLogger(__name__)
    pass
