apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
bases:
  - "../base"
nameSuffix: "-nudging"
configMapGenerator:
- files:
    - end_to_end.yaml
    - prognostic_run.yaml
    - nudging_run.yaml
    - train_sklearn.yaml
  name: end-to-end
  literals:
    - JOBNAME=nudging
  behavior: merge
