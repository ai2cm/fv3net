apiVersion: batch/v1
kind: Job
metadata:
  name: end-to-end
spec:
  template:
    spec:
      volumes:
        - name: time-volume
          configMap:
            name: fine-res-times-20200602
      containers:
        - name: integration-test
          volumeMounts:
            - mountPath: /etc/times
              name: time-volume
